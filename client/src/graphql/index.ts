import * as workSessionMutation from './mutations/workSession'; 
import * as workSessionQuery from './queries/workSession.query';

export const mutations = {
    ...workSessionMutation
}

export const queries = {
    ...workSessionQuery
}